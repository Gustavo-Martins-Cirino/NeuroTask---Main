# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables (segredos — nunca commitar)
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store
*.tsbuildinfo
.vercel

# Backend removido (scaffold FastAPI não usado) — só sobrou venv + bytecode
/backend/
__pycache__/
*.pyc

# Downloads de referência que chegam com credencial no cabeçalho. O repositório
# é público: um `git add -A` distraído publicaria a chave para sempre (reescrever
# o histórico depois não apaga o que já foi clonado). As referências VERSIONADAS
# moram em frontend/components/inspirações/ — esta pasta aqui é a de rascunho.
/frontend/inspirações/
